version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd FE
        - npm ci
    build:
      commands:
        - cd FE
        - npm run build
  artifacts:
    baseDirectory: FE/dist
    files:
      - '**/*'
  cache:
    paths:
      - FE/node_modules/**/*

